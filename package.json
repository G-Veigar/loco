{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "lerna bootstrap --hoist",
    "clean": "lerna clean",
    "dev:editor": "lerna run --scope @loco/editor serve --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
